# Let a struct literal or a match arm stay on one line up to the full width. The
# tables in `fold.rs` and `translit.rs` are the reason: one mapping per line reads as a
# table, and rustfmt's default heuristics break them into paragraphs.
use_small_heuristics = "Max"
